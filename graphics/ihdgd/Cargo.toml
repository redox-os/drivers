[package]
name = "ihdgd"
version = "0.1.0"
edition = "2021"

[dependencies]
#TODO: edid is abandoned, fork it an maintain?
edid = "0.3.0"
log = "0.4"
redox-daemon = "0.1"
redox-scheme = "0.6.2"
redox_syscall = "0.5"

common = { path = "../../common" }
pcid = { path = "../../pcid" }
libredox = "0.1.3"
