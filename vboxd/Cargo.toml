[package]
name = "vboxd"
version = "0.1.0"
edition = "2018"

[dependencies]
orbclient = "0.3.27"
# TODO: Use the latest redox_event and redox_syscall crates.
redox_event = { git = "https://gitlab.redox-os.org/redox-os/event.git", rev = "c31e3d3d" }
redox_syscall = "0.1"
