[package]
name = "usbhubd"
version = "0.1.0"
edition = "2018"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = "0.4"
redox_syscall = "0.5"
xhcid = { path = "../xhcid" }

common = { path = "../../common" }
